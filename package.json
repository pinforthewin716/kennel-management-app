{
  "name": "poodleos-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "poodleOS — kennel-management app (a better Gingr). Booking + vaccine compliance + billing.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
